const ReactNativeSystemNavigation = require('./NativeReactNativeSystemNavigation').default;

export function multiply(a: number, b: number): number {
  return ReactNativeSystemNavigation.multiply(a, b);
}
